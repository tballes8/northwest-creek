# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
websockets

# Database
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10

# Pydantic
pydantic==2.10.3
pydantic-settings==2.6.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# Environment
python-dotenv==1.0.1

# Stripe
stripe==11.2.0

# API Clients
httpx==0.28.1
requests==2.32.3

# Data Processing (skip pandas if not needed)
pandas==2.2.3  # Only if you actually use it
# numpy==2.2.1   # Only if you actually use it

# Utilities
python-dateutil==2.9.0
pytz==2024.2
email-validator==2.3.0
sendgrid
greenlet
massive
yfinance